{
  "_comment": "L4D2 Stats Point Calculation Rules - Matches SourceMod plugin logic",
  "_version": "1.0.0",
  "_last_updated": "2025-07-14",
  
  "point_values": {
    "_description": "Point values for different actions (matches SourceMod plugin)",
    
    "positive_actions": {
      "common_kill": 1,
      "headshot": 2,
      "special_kill": 6,
      "tank_kill_max": 100,
      "tank_kill_solo": 20,
      "tank_kill_melee": 50,
      "witch_kill": 15,
      "heal_teammate": 40,
      "heal_teammate_critical": 60,
      "revive_teammate": 25,
      "defib_teammate": 50,
      "teammate_save": 20,
      "ammo_pack_deploy": 20,
      "finale_win": 1000
    },
    
    "penalties": {
      "friendly_fire_per_damage": -1,
      "teammate_kill": -500
    }
  },
  
  "calculation_rules": {
    "_description": "Rules for point calculation during recalculation",
    
    "session_requirements": {
      "minimum_duration": 60,
      "require_valid_timestamps": true
    },
    
    "approximations": {
      "_description": "Approximations for data not directly tracked in sessions",
      "tank_hp_estimate": 6000,
      "teammate_save_ratio": 0.3,
      "critical_heal_ratio": 0.2
    },
    
    "limits": {
      "minimum_session_points": 0,
      "maximum_session_points": 10000
    }
  }
}